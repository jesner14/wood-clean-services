import { PlaceholderPanel } from '../../components/app/PlaceholderPanel';

export function AdminQuoteItemsPage() {
  return (
    <PlaceholderPanel
      heading="Lignes de devis"
      description="Créez et modifiez les prestations du catalogue (table quote_items) : libellé, prix unitaire, unité. Interface CRUD à venir."
    />
  );
}
