import { PlaceholderPanel } from '../../components/app/PlaceholderPanel';

export function AdminInvoicesPage() {
  return (
    <PlaceholderPanel
      heading="Factures"
      description="Génération et suivi des factures à partir des devis validés."
    />
  );
}
