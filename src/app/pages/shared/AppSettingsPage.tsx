import { PlaceholderPanel } from '../../components/app/PlaceholderPanel';

export function AppSettingsPage() {
  return (
    <PlaceholderPanel
      heading="Paramètres"
      description="Notifications, langue et préférences de sécurité seront configurables ici."
    />
  );
}
